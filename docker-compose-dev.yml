services:
  mibodeguita_app:
    build:
      context: .
      dockerfile: Dockerfile-php.dev
    ports:
      - "80:80"
    develop:
      watch:
        - action: sync
          path: .
          target: /var/www/html/MiBodeguita
#    links:
#      - db
    volumes:
      - filesdata:/var/www/html/MiBodeguita:cached
#    depends_on:
#      db:
#        condition: service_healthy

#  db:
#    image: mysql:8.0
#    environment:
#      MYSQL_ROOT_PASSWORD: rexar151
#      MYSQL_PASSWORD: rexar151
#      MYSQL_DATABASE: mibodeguita_db
#    ports:
#      - "3308:3306"
#    volumes:
#      - dbdata:/var/lib/mysql
#    healthcheck:
#      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
#      timeout: 10s
#      retries: 10

volumes:
#  dbdata:
  filesdata: